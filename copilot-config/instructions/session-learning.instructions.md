---
description: "Lean session learning loop: search before complex actions, log outcomes after significant actions."
applyTo: "git-*,scripts/**,copilot-config/**,vscode-extension/**"
---

# Session Learning (Lean)

For non-trivial tasks:

1. Search recent session memory for prior failures.
2. Avoid repeating failed approaches.
3. Log significant outcomes (especially surprises and failures).

Keep logs concise and useful.
