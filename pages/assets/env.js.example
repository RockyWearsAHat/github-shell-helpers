/* Atlas Practice — Environment config (TEMPLATE)
 * Copy this file to env.js and fill in your values.
 * env.js is gitignored and will not be committed.
 *
 * After deploying atlas-api/ to Netlify, paste your site URL below.
 */
window.ATLAS_API_BASE = "";
